{
  "name": "dietetica-san-jose",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm --prefix frontend run build",
    "build:backend:exe": "npm run build:frontend && npm --prefix backend run pkg:win"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "bin": "backend/dist/main.js",
  "pkg": {
    "assets": [
      "backend/node_modules/better-sqlite3/build/Release/better_sqlite3.node",
      "backend/public/**/*"
    ],
    "targets": [
      "node20-win-x64"
    ]
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^5.15.0",
    "ts-loader": "^9.5.4"
  }
}
